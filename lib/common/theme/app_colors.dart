import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Color.fromARGB(255, 5, 13, 126);
  static Color? backgroundColor = Colors.grey[200];
  static Color? whiteColor = Colors.white54;
}
